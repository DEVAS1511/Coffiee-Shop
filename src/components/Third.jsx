import React from 'react'

const Third = () => {
  return (
    <div>
      <h3>What is React? 
React was created by Facebook (Meta).It helps developers build fast, interactive, and reusable UI components.
Key Concepts of React
1. Component
Small, reusable parts of the UI
Example: Header, Footer, Button

2. JSX
A syntax that looks like HTML inside JavaScript
Makes code easy to read and write

3. Virtual DOM
React uses a virtual copy of the real DOM
Updates are faster and performance is better

4. Props
Used to pass data from parent to child components

5. State
Stores data inside a component
Changes in state update the UI automatically

6. Hooks
Special functions like useState and useEffect
Used in functional components</h3>
    </div>
  )
}

export default Third
